#include <stdio.h>
#include <stdlib.h>

//Write c code to sum numbers from 1 to 100 (without
//loop).

int main()
{
    int sum = 0;

    sum = (100*(100+1))/2;

    printf("the sum = %d",sum);

    return 0;
}